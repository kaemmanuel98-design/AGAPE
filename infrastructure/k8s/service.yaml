apiVersion: v1
kind: Service
metadata:
  name: agape-web
  namespace: agape
  labels:
    app.kubernetes.io/name: agape
spec:
  type: LoadBalancer
  selector:
    app: agape-web
  ports:
    - name: http
      port: 80
      targetPort: http
