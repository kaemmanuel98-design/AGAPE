apiVersion: apps/v1
kind: Deployment
metadata:
  name: agape-web
  namespace: agape
  labels:
    app.kubernetes.io/name: agape
    app.kubernetes.io/component: web
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: agape-web
  template:
    metadata:
      labels:
        app: agape-web
        app.kubernetes.io/name: agape
    spec:
      securityContext:
        fsGroup: 1001
      containers:
        - name: web
          # Remplacé par le workflow GitHub Actions (sed sur AGAPE_IMAGE_PLACEHOLDER).
          image: AGAPE_IMAGE_PLACEHOLDER
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: agape-config
            - secretRef:
                name: agape-secrets
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 25
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/ready
              port: http
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 150m
              memory: 384Mi
            limits:
              cpu: "1500m"
              memory: 1536Mi
